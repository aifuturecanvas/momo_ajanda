name: momo_ajanda
description: Momo Akıllı Ajanda Uygulaması
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.4.9
  
  # Supabase
  supabase_flutter: ^2.3.0
  
  # HTTP (OpenAI için)
  http: ^1.1.0
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # UI & Charts
  fl_chart: ^0.63.0
  google_fonts: ^6.1.0
  card_swiper: ^3.0.1
  table_calendar: ^3.0.9
  
  # Voice
  speech_to_text: ^6.6.0
  flutter_tts: ^3.8.5
  
  # Notifications
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2
  
  # Utils
  uuid: ^4.2.1
  intl: ^0.18.1
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1